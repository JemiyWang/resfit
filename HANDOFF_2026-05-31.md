# ResFiT 复现 — Handoff (2026-05-31, 新增 4 个 dexmg 任务 + 修复 AV1 坏帧)

> 接续 `HANDOFF.md`(2026-05-30 晚, Phase 2 RL)。本篇只覆盖 5-31 这一轮新增的工作。
> 环境/约定不变:conda env **`residual`**,仓库 `/data2/RL/residual-offpolicy-rl`,**所有命令从仓库根目录跑**(代码靠 cwd 导入,resfit 没装成包),eval 离屏渲染要 `MUJOCO_GL=egl`。

## 0. 一句话现状
**给原本 3 个 dexmg 任务之外,又补齐了 4 个任务(DrawerCleanup / ThreePieceAssembly / TwoArmThreading / TwoArmTransport)的完整两阶段脚手架;并修掉了一个会让 BC 必崩的 AV1 坏帧 bug。**
- 现在在跑:**piece BC(GPU6)**、**drawer BC(GPU7)**,均已稳过 step 1400+、零崩溃。
- threading / transport:脚手架就绪,**尚未开训**。

---

## 1. 新增的 4 个任务

| task(简称) | eval_env | 数据集 | 相机 | 备注 |
|---|---|---|---|---|
| piece | TwoArmThreePieceAssembly | ankile/dexmg-two-arm-three-piece-assembly | agentview + robot0/1_eye_in_hand | horizon 500 |
| drawer | TwoArmDrawerCleanup | ankile/dexmg-two-arm-drawer-cleanup | 同上 | 给 dexmg.py horizon 表补了 500 |
| twoarmthreading | TwoArmThreading | ankile/dexmg-two-arm-threading | 同上 | horizon 300 |
| transport | TwoArmTransport(alias Transport) | **ankile/dexmg-two-arm-transport** | **shouldercamera0/1**(非 agentview) | 见下方 ⚠️ |

⚠️ **transport 三个特殊点**(都已处理好):
1. 相机是 `shouldercamera0/1`,不是 agentview;该数据集含 5 个相机,故 BC 加了 `--policy_cameras shouldercamera0 shouldercamera1` 过滤,RL config 的 `rl_camera` 也只取这俩。
2. `env_configuration="opposed"` —— `dexmg.py` 按 env 名自动处理,无需改。
3. 长 horizon(800),gamma 用 0.998(在 paper_runs 脚本里给)。

每个任务都加了三处(全在仓库内,git 可查):
- **残差 RL config**:`resfit/rl_finetuning/config/residual_td3.py` 里 `ResidualTD3{ThreePieceAssembly,DrawerCleanup,TwoArmThreading,TwoArmTransport}Config`(都继承 BoxClean,total_timesteps=500k;transport 覆盖 rl_camera),已 `cs.store` 注册为 `residual_td3_{three_piece_assembly,drawer_cleanup,two_arm_threading,two_arm_transport}_config`。
- **RL 启动脚本**:`resfit/rl_finetuning/shell/paper_runs/{pieceassembly,drawercleanup,twoarmthreading,transport}/1_*_residual_rl.sh`。
- **BC 启动脚本**:`resfit/lerobot/shell/launch_act_*.sh`(从裸版改成完整版:自带 `cd 仓库根 + MUJOCO_GL=egl + conda run`,steps=200000;修了 piece/threading 的 video_key frontview→agentview、transport→shouldercamera0)。
- 另加了 BC 统一 wrapper **`resfit/lerobot/shell/run_bc.sh <task> [gpu]`**(run_rl.sh 的 BC 版:解析脚本→tmux 后台→tee 日志→防同名双开)。

---

## 2. ⚠️ 关键修复:AV1 坏帧导致 BC 必崩(非显然,务必了解)

**现象**:BC 训练随机几百步内崩,报 `RuntimeError: Could not push packet to decoder: Invalid data found`(torchcodec/libdav1d)。

**排查结论(纠正了几个误判)**:
- 所有 dexmg 数据集(**含已训成功的 boxcleanup/coffee**)都是 **AV1 编码**,不是 H.264;问题不在编码格式,转码不是方向。env 的 ffmpeg 也支持 AV1。
- 真因:**少量 mp4 是源文件坏帧**(重下字节一样还是坏;libdav1d 和 libaom 都报 `Corrupt frame`)。torchcodec 全量逐帧扫描定位:piece 坏 episode `{540,709,825,862}`、drawer `{505}`。训练 DataLoader 随机采到坏帧 → worker 抛异常 → 整轮崩。

**走过的弯路(都已撤回,别再走)**:
- `--video_backend pyav`:只是当时没采到坏 episode 的运气,非根治。
- 用 LeRobotDataset `episodes=` 排除坏集:撞 lerobot 非连续子集 bug → `IndexError: index N out of bounds for size M`(M=过滤后集数,它没重映射 episode_index)。

**最终修法(已上线、已验证)**:
`resfit/lerobot/scripts/train_bc_dexmg.py` 加了模块级包装器 **`_SkipBadVideoDataset`**:`__getitem__` 解码失败就随机重采一个有效样本(≤20 次)。
- 用**全量数据集**(episodes=None)→ 不触发 lerobot 子集 bug;
- 运行时**跳过任意坏帧** → 不用枚举、不用扫描,**threading/transport 以及未来所有 AV1 数据集自动覆盖**;
- 坏帧占比 <0.3%,对 BC 基座无影响。
- 隔离测试通过(坏帧 base 报错、包装器返回正常样本);piece/drawer 实跑 step 1400+ 零崩。

诊断工具:`resfit/lerobot/shell/scan_bad_episodes.sh <hf-repo-id>`(ffmpeg 全扫输出坏 episode 列表,**训练用不到**,只用于查证)。

---

## 3. 当前运行状态(2026-05-31 ~15:18)
| 任务 | GPU | tmux | 日志 | 状态 |
|---|---|---|---|---|
| piece BC | 6 | piece_bc | piece_bc_train.log | step 1500/200000,loss~0.21,零崩 |
| drawer BC | 7 | drawer_bc | drawer_bc_train.log | step 1400/200000,loss~0.24,零崩 |
| coffee BC | 4 | coffee_bc | coffee_bc_train.log | 续训中(上一轮的事,见 memory) |
| 原 5 任务 RL | 0/1/2/5 | *_rl | *_rl_train.log | 见旧 HANDOFF.md |

监控:`tmux attach -t piece_bc`(Ctrl-b d 退出)或 `tail -f piece_bc_train.log`;曲线看 wandb `dexmg-threepiece-bc` / `dexmg-drawercleanup-bc` 的 `eval/success_rate`。

---

## 4. 怎么继续(每个任务两阶段)

**① BC**(挑空闲卡;piece/drawer 已在跑,threading/transport 待起):
```bash
cd /data2/RL/residual-offpolicy-rl
bash resfit/lerobot/shell/run_bc.sh twoarmthreading <gpu>
bash resfit/lerobot/shell/run_bc.sh transport <gpu>
```
盯 wandb `eval/success_rate`,**等出现 >0% 的 New best**(才会存 `_best` artifact)。

**② 填 wandb_id**:BC 出 `_best` 后,把它的 `wandb项目/run_id` 填进 `resfit/rl_finetuning/config/residual_td3.py` 里对应 config 的 `base_policy.wandb_id`(现都是占位 `TODO_FILL_BC_RUN_ID`,共 4 处:
`dexmg-threepiece-bc/…`、`dexmg-drawercleanup-bc/…`、`dexmg-twoarmthreading-bc/…`、`dexmg-transport-bc/…`)。

**③ 残差 RL**:
```bash
bash resfit/rl_finetuning/shell/run_rl.sh pieceassembly <gpu>
bash resfit/rl_finetuning/shell/run_rl.sh drawercleanup <gpu>
bash resfit/rl_finetuning/shell/run_rl.sh twoarmthreading <gpu>
bash resfit/rl_finetuning/shell/run_rl.sh transport <gpu>
```

---

## 5. 最大风险(同 Coffee 那条)
这些都是较难的双臂任务,**BC 基座可能长期 0% 成功**(Coffee 之前就栽在这,后来发现是 eval 分辨率喂错,见 memory `project_resfit_coffee_eval_fix`)。0% 就不会存 `_best`,残差 RL 拉不到基座起不来。**先确认 BC 的 `eval/success_rate` 出现非 0,再进 RL。**

---

## 6. 本轮改动的文件速查
- `resfit/lerobot/scripts/train_bc_dexmg.py` —— 加 `_SkipBadVideoDataset` 包装器(坏帧重采)。
- `resfit/rl_finetuning/config/residual_td3.py` —— 4 个新任务的残差 config + 注册。
- `resfit/rl_finetuning/shell/paper_runs/{pieceassembly,drawercleanup,twoarmthreading,transport}/1_*_residual_rl.sh` —— 新建。
- `resfit/lerobot/shell/launch_act_{twoarmthreepieceassembly,drawercleanup,twoarmthreading,transport}.sh` —— 完整版 + video_key/policy_cameras 修正。
- `resfit/lerobot/shell/run_bc.sh` —— 新建(BC 统一启动 wrapper)。
- `resfit/lerobot/shell/scan_bad_episodes.sh` —— 新建(坏帧诊断,可选)。
- `resfit/dexmg/environments/dexmg.py` —— horizon 表加 `TwoArmDrawerCleanup: 500`。
- 项目 memory:`project_resfit_two_new_tasks.md`(含本轮全部要点)。
